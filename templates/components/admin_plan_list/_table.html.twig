{% if filteredPlans is empty %}
    <div class="bg-white rounded-2xl border border-gray-200 shadow-sm px-6 py-16 text-center">
        <svg class="mx-auto w-10 h-10 text-gray-300 mb-3" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414A1 1 0 0119 9.414V19a2 2 0 01-2 2z"/>
        </svg>
        <p class="text-sm font-medium text-gray-500">No plans match your search</p>
        <p class="text-xs text-gray-400 mt-1">Try adjusting the filters above</p>
    </div>
{% else %}
    <div class="bg-white rounded-2xl border border-gray-200 shadow-sm overflow-hidden">
        <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-100">
                <thead>
                    <tr class="bg-gray-50">
                        <th class="px-4 sm:px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wide">Patient</th>
                        <th class="px-4 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wide hidden sm:table-cell">IDs</th>
                        <th class="px-4 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wide hidden sm:table-cell">Plan</th>
                        <th class="px-4 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wide">Status</th>
                        <th class="px-4 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wide hidden md:table-cell">Next Order</th>
                        <th class="px-4 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wide hidden md:table-cell">Next Billing</th>
                        <th class="px-4 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wide hidden lg:table-cell">Last Change</th>
                        <th class="px-4 sm:px-6 py-3 text-right text-xs font-semibold text-gray-500 uppercase tracking-wide"></th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-gray-100">
                    {% for plan in filteredPlans %}
                        <tr class="hover:bg-gray-50 transition-colors">

                            {# Patient #}
                            <td class="px-4 sm:px-6 py-4">
                                <p class="text-sm font-semibold text-gray-900">{{ plan.patientName }}</p>
                                <p class="text-xs text-gray-500 mt-0.5">{{ plan.patientEmail }}</p>
                                <p class="text-xs text-gray-400">{{ plan.patientPhone }}</p>
                            </td>

                            {# IDs #}
                            <td class="px-4 py-4 hidden sm:table-cell">
                                <p class="text-xs font-mono text-gray-500">{{ plan.planId }}</p>
                                <p class="text-xs font-mono text-gray-400">{{ plan.customerId }}</p>
                            </td>

                            {# Plan #}
                            <td class="px-4 py-4 hidden sm:table-cell max-w-[180px]">
                                <p class="text-sm text-gray-900 truncate" title="{{ plan.planName }}">{{ plan.planName }}</p>
                                <p class="text-xs text-gray-500 mt-0.5 truncate" title="{{ plan.medication }} {{ plan.variant }}">{{ plan.medication }} {{ plan.variant }}</p>
                                <div class="flex items-center gap-1.5 mt-1">
                                    <span class="inline-flex items-center px-1.5 py-0.5 rounded text-xs font-medium bg-gray-100 text-gray-500">{{ plan.category }}</span>
                                    <span class="text-xs text-gray-400">{{ plan.ordersCompleted }}/{{ plan.ordersTotal }}</span>
                                </div>
                            </td>

                            {# Status badge #}
                            <td class="px-4 py-4">
                                {% if plan.status == 'active' %}
                                    <span class="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full text-xs font-medium bg-green-50 text-green-700 border border-green-200">
                                        <span class="w-1.5 h-1.5 rounded-full bg-green-500 flex-shrink-0"></span>active
                                    </span>
                                {% elseif plan.status == 'requested-renew' %}
                                    <span class="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full text-xs font-medium bg-amber-50 text-amber-700 border border-amber-200">
                                        <span class="w-1.5 h-1.5 rounded-full bg-amber-500 flex-shrink-0"></span>requested-renew
                                    </span>
                                {% elseif plan.status == 'requested-change' %}
                                    <span class="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full text-xs font-medium bg-blue-50 text-blue-700 border border-blue-200">
                                        <span class="w-1.5 h-1.5 rounded-full bg-blue-500 flex-shrink-0"></span>requested-change
                                    </span>
                                {% elseif plan.status == 'requested-approve' %}
                                    <span class="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full text-xs font-medium bg-purple-50 text-purple-700 border border-purple-200">
                                        <span class="w-1.5 h-1.5 rounded-full bg-purple-500 flex-shrink-0"></span>requested-approve
                                    </span>
                                {% elseif plan.status == 'deactivated' %}
                                    <span class="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full text-xs font-medium bg-red-50 text-red-700 border border-red-200">
                                        <span class="w-1.5 h-1.5 rounded-full bg-red-500 flex-shrink-0"></span>deactivated
                                    </span>
                                {% elseif plan.status == 'expired' %}
                                    <span class="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full text-xs font-medium bg-gray-100 text-gray-500 border border-gray-200">
                                        <span class="w-1.5 h-1.5 rounded-full bg-gray-400 flex-shrink-0"></span>expired
                                    </span>
                                {% else %}
                                    <span class="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full text-xs font-medium bg-gray-100 text-gray-500 border border-gray-200">
                                        <span class="w-1.5 h-1.5 rounded-full bg-gray-400 flex-shrink-0"></span>{{ plan.status }}
                                    </span>
                                {% endif %}
                            </td>

                            {# Next order #}
                            <td class="px-4 py-4 hidden md:table-cell">
                                {% if plan.nextOrderDate %}
                                    <p class="text-sm text-gray-900">{{ plan.nextOrderDate|date('d M Y') }}</p>
                                {% else %}
                                    <p class="text-sm text-gray-400">—</p>
                                {% endif %}
                            </td>

                            {# Next billing #}
                            <td class="px-4 py-4 hidden md:table-cell">
                                {% if plan.nextBillingDate %}
                                    <p class="text-sm text-gray-900">{{ plan.nextBillingDate|date('d M Y') }}</p>
                                {% else %}
                                    <p class="text-sm text-gray-400">—</p>
                                {% endif %}
                            </td>

                            {# Last change #}
                            <td class="px-4 py-4 hidden lg:table-cell">
                                {% if plan.activity is not empty %}
                                    <p class="text-xs text-gray-700 max-w-[200px] leading-snug">{{ plan.activity[0].description }}</p>
                                    <p class="text-xs text-gray-400 mt-0.5">{{ plan.activity[0].date|date('d M') }} · {{ plan.activity[0].agent }}</p>
                                {% else %}
                                    <p class="text-sm text-gray-400">—</p>
                                {% endif %}
                            </td>

                            {# View link #}
                            <td class="px-4 sm:px-6 py-4 text-right">
                                <a
                                    href="{{ path('admin_plan_detail', {id: plan.id}) }}"
                                    class="inline-flex items-center gap-1.5 px-3 py-1.5 text-xs font-medium text-gray-700 bg-white border border-gray-200 rounded-lg hover:bg-gray-50 hover:border-gray-300 transition-colors"
                                >
                                    View
                                    <svg class="w-3 h-3" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/>
                                    </svg>
                                </a>
                            </td>

                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>

        {# Footer note — real implementation would paginate #}
        <div class="px-4 sm:px-6 py-3 border-t border-gray-100 bg-gray-50">
            <p class="text-xs text-gray-400">
                Showing {{ filteredPlans|length }} of {{ filteredPlans|length }} plans
                <span class="text-gray-300 mx-1">·</span>
                Pagination would be implemented here in production
            </p>
        </div>
    </div>
{% endif %}
